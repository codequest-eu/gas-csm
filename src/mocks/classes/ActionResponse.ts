import { BaseClass } from "../../base/BaseClass";

// instances of this class should never be created directly in user code
// only by ActionResponseBuilder
export class ActionResponse extends BaseClass {}
